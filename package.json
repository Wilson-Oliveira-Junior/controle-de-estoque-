{
  "name": "controle-estoque",
  "version": "1.0.0",
  "description": "Sistema de controle de estoque escolar com executável.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package-win": "electron-packager . ControleEstoque --platform=win32 --arch=x64 --out=dist --overwrite"
  },
  "devDependencies": {
    "electron": "^26.0.0",
    "electron-packager": "^17.0.0"
  }
}
